import { UserInterface } from "../types/user";

export const generateAccessToken = (user: UserInterface) => {};
export const refreshAccessToken = (tokrn: string) => {};
export const verifyAccessToken = (token: string) => {};
